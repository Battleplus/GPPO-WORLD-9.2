# 指定服务器连接诊断

日期：2026-09-05。目标服务器 `172.17.27.173`，账号名称已由用户提供；认证密码未写入仓库、日志或迁移包。

多次通过 SSH 连接默认端口 22，均在身份验证前超时，因此密码未被发送。另测试常见 SSH 端口 2200、2222、8022，均不可达。

本机路由表中没有到 `172.17.27.173` 或其网段的路由。深信服 EasyConnect 进程正在运行，但对应虚拟网卡“本地连接 2”状态为 `Disconnected`，也没有下发 172.17 网段路由。当前现象属于网络/VPN 通道未建立，不是 SSH 密码错误或训练程序失败。

按用户要求，本机训练已经停止，后续训练程序强制要求 Linux + CUDA + 服务器专用冻结协议，不能回退到本机 CPU。已完成的本机部分实验独立标记为中断并导出，不参与服务器 CUDA 矩阵。

解除阻碍需要以下任一条件：

1. 在 EasyConnect 中完成对应网络的登录，使虚拟网卡连接并下发 `172.17.27.173` 的路由；
2. 连接服务器所在的校园网/局域网；
3. 提供可达的跳板机、SSH 端口或外网地址。

## 后续恢复记录

同日 VPN 路由恢复后，`172.17.27.173:22` 可达并完成认证。服务器 `ru-server-MS-1` 的两张 RTX 2080 Ti 在预检时均无计算进程；正式实验选用 GPU 0，未终止或抢占任何既有任务。J-02B 已在服务器完成并导出，详见 [服务器实验报告](server-run1/final-report.md)。本节前述内容保留为连接故障的实际历史记录。
